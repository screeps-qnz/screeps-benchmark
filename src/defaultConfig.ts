import { ParsedSLConfig } from "setup";

export const serverDefaultConfig: ParsedSLConfig = {
  "steamKey": "",
  "mods": [
    "screepsmod-auth",
    "screepsmod-admin-utils"
  ],
  "bots": {
  },
  "serverConfig": {
    "tickRate": 100
  }
}
